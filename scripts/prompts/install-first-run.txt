A horizontal strip of three equal panels, read left to right, joined by two thin warm arrows. Each panel shows the same rounded cell wall made of thick vertical bars in the warm gold-to-magenta gradient.

FIRST PANEL (do not write the word "stage" or any number anywhere), with the single heading "Dry run": the cell wall is drawn only as a dashed outline, a plan that has not been built yet. The cyan microchip sits OUTSIDE the dashed cell, to its left, with exactly one label under it: "Agent". No other word near the chip. Nothing is inside the dashed cell.

SECOND PANEL, with the single heading "Look around": the cell wall is solid. Inside the cell there is a small terminal window showing only a prompt cursor, and one cyan folder labeled "Project". There is no microchip in this panel.

THIRD PANEL, with the single heading "Run the agent": the cell wall is solid. Inside the cell sit the cyan microchip labeled "Agent" and the cyan folder labeled "Project". Two narrow openings in the wall glow gold: one on top with a small globe icon labeled "Network", one on the right with a small key-card icon labeled "Login". A thin cyan line goes from the Agent chip through each opening.

Below all three panels, outside every wall, a single dim barred folder labeled "Your home" stays out of reach in every panel.

The complete list of words allowed in the image: "Dry run", "Look around", "Run the agent", "Agent", "Project", "Network", "Login", "Your home". Nothing else is written anywhere.
